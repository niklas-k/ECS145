#used so that each thread shared memory
library(bigmemory)

#start of simulator
newsim() <- function(processVec, endOfSim) {
    #creation of OS, managaes time and waking up each thread
    

    #creation of a thread for each item in processVec and manages each process
}